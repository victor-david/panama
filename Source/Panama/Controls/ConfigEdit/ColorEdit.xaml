<UserControl 
    x:Class="Restless.App.Panama.Controls.ColorEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:xc="http://schemas.xceed.com/wpf/xaml/toolkit"
    mc:Ignorable="d"
    Name="PART_Control"
    d:DesignHeight="40" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.75*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Column="0" Grid.Row="0" 
            Style="{StaticResource TextBlockFolderTitle}"
            VerticalAlignment="Center"
            Text="{Binding Title, ElementName=PART_Control}"/>
        <xc:ColorPicker 
            x:Name="ColorPicker"
            Grid.Column="1" Grid.Row="0" 
            Margin="2,4"
            FontSize="11" FontFamily="Verdana" 
            SelectedColor="{Binding SelectedColor, ElementName=PART_Control}"/>
    </Grid>
</UserControl>
