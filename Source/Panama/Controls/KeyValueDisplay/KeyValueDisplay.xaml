<UserControl 
    x:Class="Restless.App.Panama.Controls.KeyValueDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    Name="UC"
    d:DesignHeight="48" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Label 
            Foreground="{Binding HeaderForeground,ElementName=UC}"
            Width="{Binding HeaderWidth,ElementName=UC}"
            FontSize="{Binding HeaderFontSize,ElementName=UC}"
            Content="{Binding Header,ElementName=UC}"
            VerticalAlignment="Center"/>
        <TextBlock Text="{Binding Value,ElementName=UC}" FontSize="11" VerticalAlignment="Center"/>
    </StackPanel>
</UserControl>
