<UserControl 
    x:Class="Restless.App.Panama.Controls.KeyValueDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    Name="UC"
    d:DesignHeight="148" d:DesignWidth="300">
    <DockPanel>
        <Label
            DockPanel.Dock="Left"
            Foreground="{Binding HeaderForeground,ElementName=UC}"
            Width="{Binding HeaderWidth,ElementName=UC}"
            FontSize="{Binding HeaderFontSize,ElementName=UC}"
            Content="{Binding Header,ElementName=UC}"
            VerticalAlignment="{Binding HeaderVerticalAlignment,ElementName=UC}"/>

        <TextBlock
            DockPanel.Dock="Left"
            Foreground="{Binding ValueForeground,ElementName=UC}"
            Text="{Binding Value,ElementName=UC}"
            FontSize="{Binding ValueFontSize,ElementName=UC}"
            TextWrapping="Wrap"
            VerticalAlignment="{Binding ValueVerticalAlignment,ElementName=UC}" />
    </DockPanel>
</UserControl>
