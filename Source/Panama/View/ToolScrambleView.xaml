<UserControl 
    x:Class="Restless.App.Panama.View.ToolScrambleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:view="clr-namespace:Restless.App.Panama.View"
    xmlns:con="clr-namespace:Restless.App.Panama.Controls"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:r="clr-namespace:Restless.App.Panama.Resources"
    mc:Ignorable="d"
    d:DesignWidth="860" d:DesignHeight="400">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top"  Style="{StaticResource TextBlockMessage}" Text="{x:Static r:Strings.HeaderToolOperationScramble}"/>
        <StackPanel DockPanel.Dock="Top" Style="{StaticResource StackPanelCommandHolder}">
            <Button Command="{Binding Commands[Paste]}" Content="Paste"/>
            <Button Command="{Binding Commands[Begin]}" Content="Scramble"/>
            <CheckBox IsChecked="{Binding ScrambleWords}">Scramble words</CheckBox>
        </StackPanel>
        
        <TextBox DockPanel.Dock="Top" Style="{StaticResource TextBoxMultiFull}" Margin="0,12,0,0" Text="{Binding Text}"/>

    </DockPanel>
</UserControl>