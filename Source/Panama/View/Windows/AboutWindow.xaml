<Window 
    x:Class="Restless.App.Panama.View.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:con="clr-namespace:Restless.App.Panama.Controls"
    xmlns:r="clr-namespace:Restless.App.Panama.Resources"
    Title="About" 
    Height="440" Width="586" 
    ResizeMode="NoResize" 
    ShowInTaskbar="False" 
    WindowStartupLocation="CenterScreen">
    
    <Grid Background="{StaticResource HorizontalLightBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="StackPanel">
                <Setter Property="Margin" Value="4"/>
            </Style>          
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="4"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>
            <Style TargetType="con:KeyValueDisplay">
                <Setter Property="Margin" Value="0"/>
                <Setter Property="HeaderFontSize" Value="13"/>
                <Setter Property="HeaderWidth" Value="84"/>
                <Setter Property="HeaderForeground" Value="DarkBlue"/>
                <Setter Property="HeaderVerticalAlignment" Value="Top"/>
                <Setter Property="ValueFontSize" Value="13"/>
                <Setter Property="ValueForeground" Value="#FF818181"/>
                <Setter Property="ValueVerticalAlignment" Value="Top"/>
            </Style>
        </Grid.Resources>
        <StackPanel Grid.Column="0">

            <con:KeyValueDisplay Header="Product">
                <con:KeyValueDisplay.Value>
                    <MultiBinding StringFormat="{}{0} {1}" Mode="OneTime">
                        <Binding Path="AppInfo.Assembly.Title" />
                        <Binding Path="AppInfo.Assembly.VersionMajor" />
                    </MultiBinding>
                </con:KeyValueDisplay.Value>
            </con:KeyValueDisplay>
            
            <con:KeyValueDisplay Header="Build Date" Value="{Binding AppInfo.BuildDate, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="Company" Value="{Binding AppInfo.Assembly.Company, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="Copyright" Value="{Binding AppInfo.Assembly.Copyright, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="Description" Value="{Binding AppInfo.Assembly.Description, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="Framework" Value="{Binding AppInfo.Assembly.FrameworkVersion, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="Executable" Value="{Binding AppInfo.Assembly.Location, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="64 Bit" Value="{Binding AppInfo.Is64Bit, Mode=OneTime}"/>
            <con:KeyValueDisplay Header="Database" Value="{Binding AppInfo.DatabaseFileName, Mode=OneTime}"/>
            
            <Grid Margin="16,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Image Source="{StaticResource ImageTitle}" Height="64"/>
                <TextBlock Margin="8,0" Grid.Column="1" Text="{x:Static r:Strings.CaptionIconsDesigned1}"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
