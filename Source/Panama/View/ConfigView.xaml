<UserControl 
    x:Class="Restless.App.Panama.View.ConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:con="clr-namespace:Restless.App.Panama.Controls"
    xmlns:view="clr-namespace:Restless.App.Panama.View"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cv="clr-namespace:Restless.Tools.Converters;assembly=Restless.Tools.Library"
    xmlns:r="clr-namespace:Restless.App.Panama.Resources"
    xmlns:xc="http://schemas.xceed.com/wpf/xaml/toolkit"
    mc:Ignorable="d"
    d:DesignWidth="862" d:DesignHeight="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Section selector -->
        <ItemsControl Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource ItemsControlSubSelection}" ItemsSource="{Binding Sections}"/>

        <DockPanel Grid.Row="1">
            <!-- Section 1. General -->
            <StackPanel 
                DockPanel.Dock="Top"
                Margin="20,10"
                Visibility="{Binding SelectedSection, Converter={cv:Int64ToVisibilityConverter}, ConverterParameter={StaticResource Int64One}}">

                <GroupBox Header="{x:Static r:Strings.HeaderSettingsDateFormat}">
                    <StackPanel>
                        <ItemsControl Style="{StaticResource SelectTypeSingle}" ItemsSource="{Binding Path=DateFormats}" />
                        <TextBox Margin="36,-16,36,0" Text="{Binding Config.DateFormat,UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding DateFormats[3].IsSelected}"/>
                    </StackPanel>
                </GroupBox>
                <TextBlock Style="{StaticResource TextBlockMessage}" Text="{x:Static r:Strings.ConfigRowHeight}"/>
                <!-- 24 is the defined MinRowHeight in Controls.xaml -->
                <Slider Margin="2,4" Minimum="24" Maximum="42" TickPlacement="BottomRight" TickFrequency="1" Value="{Binding Path=Config.DataGridRowHeight}"/>
            </StackPanel>

            <!-- Section 2. Folders -->
            <StackPanel
                DockPanel.Dock="Top"
                Margin="20,10"
                Visibility="{Binding SelectedSection, Converter={cv:Int64ToVisibilityConverter}, ConverterParameter={StaticResource Int64Two}}">
                <con:FolderEdit Title="Title Root" Folder="{Binding Config.FolderTitleRoot}"/>
                <con:FolderEdit Title="Title Version" Folder="{Binding Config.FolderTitleVersion}"/>
                <con:FolderEdit Title="Submission Documents" Folder="{Binding Config.FolderSubmissionDocument}"/>
                <con:FolderEdit Title="Submission Message Attachments" Folder="{Binding Config.FolderSubmissionMessageAttachment}"/>
                <con:FolderEdit Title="Export" Folder="{Binding Config.FolderExport}"/>
                <con:FolderEdit Title="Mapi Messages" Folder="{Binding Config.FolderMapi}" EditType="Mapi"/>
            </StackPanel>
            
            <!-- Section 3. Colors -->
            <DockPanel 
                DockPanel.Dock="Top"
                Margin="20,10"
                Visibility="{Binding SelectedSection, Converter={cv:Int64ToVisibilityConverter}, ConverterParameter={StaticResource Int64Three}}">
                <con:ColorEdit DockPanel.Dock="Top" Title="Published Title" SelectedColor="{Binding Config.ColorPublishedTitle}"/>
                <con:ColorEdit DockPanel.Dock="Top" Title="Submitted Title" SelectedColor="{Binding Config.ColorSubmittedTitle}" />
                <con:ColorEdit DockPanel.Dock="Top" Title="Publisher In Submission Period" SelectedColor="{Binding Config.ColorPeriodPublisher}"/>
                <Button DockPanel.Dock="Top" Content="Reset" Width="80" HorizontalAlignment="Left" Command="{Binding Commands[ResetColors]}"/>
            </DockPanel>
            
            <!-- Section 4. Submission -->
            <DockPanel
                DockPanel.Dock="Top"
                Margin="20,10"
                Visibility="{Binding SelectedSection, Converter={cv:Int64ToVisibilityConverter}, ConverterParameter={StaticResource Int64Four}}">
                <Label DockPanel.Dock="Top" Content="Company"/>
                <TextBox DockPanel.Dock="Top" Text="{Binding Config.SubmissionDocCompany}"/>
                <Label DockPanel.Dock="Top" Content="Header"/>
                <TextBox DockPanel.Dock="Top" Text="{Binding Config.SubmissionDocHeader}"/>
                <con:YesNoEdit DockPanel.Dock="Top" Header="Include Page # in Header" IsYes="{Binding Config.SubmissionDocHeaderPages}" GroupName="HeaderPages"/>
                <Label DockPanel.Dock="Top" Content="Footer"/>
                <TextBox DockPanel.Dock="Top" Text="{Binding Config.SubmissionDocFooter}"/>
                <con:YesNoEdit DockPanel.Dock="Top" Header="Include Page # in Footer" IsYes="{Binding Config.SubmissionDocFooterPages}" GroupName="FooterPages"/>
                <Label DockPanel.Dock="Top" Content="Document Text"/>
                <TextBox DockPanel.Dock="Top" Style="{StaticResource TextBoxMultiFull}" Text="{Binding Config.SubmissionDocBody}"/>
                
            </DockPanel>            
            
        </DockPanel>
    </Grid>
    <!-- end main grid-->
</UserControl>