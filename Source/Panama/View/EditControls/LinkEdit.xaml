<UserControl 
    x:Class="Restless.App.Panama.View.LinkEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="229" d:DesignWidth="350">
    <Control.Resources>
        <ResourceDictionary Source="../../Resources/Merged.xaml" />
    </Control.Resources>    
    <Border>
        <StackPanel>
            <Label>Link Name</Label>
            <TextBox Text="{Binding SelectedRow[name]}" />
            <Label>Url</Label>
            <TextBox Text="{Binding SelectedRow[url]}" />
            <Label>Note</Label>
            <TextBox Text="{Binding SelectedRow[notes], TargetNullValue=''}" />
            <Button Command="{Binding RawCommands[BrowseCommand]}" Width="120" HorizontalAlignment="Left">Go to url</Button>
        </StackPanel>
    </Border>
</UserControl>
