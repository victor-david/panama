<UserControl 
    x:Class="Restless.Panama.Controls.GridRowDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    Name="PART_Control"
    d:DesignHeight="48" d:DesignWidth="400">
    <Grid Name="PART_Grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding HeaderWidth,ElementName=PART_Control}"/>
        </Grid.ColumnDefinitions>
        <Label 
            Foreground="{Binding HeaderForeground,ElementName=PART_Control}"
            Width="{Binding HeaderWidth,ElementName=PART_Control}"
            FontSize="{Binding HeaderFontSize,ElementName=PART_Control}"
            Content="{Binding Header,ElementName=PART_Control}"
            VerticalAlignment="Center"/>
    </Grid>
</UserControl>
