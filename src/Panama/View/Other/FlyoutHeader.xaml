<UserControl
    x:Class="Restless.Panama.View.FlyoutHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="clr-namespace:Restless.Panama.ViewModel"
    xmlns:tk="http://schemas.restless.toolkit.com/2021"
    d:DataContext="{d:DesignInstance Type=vm:TitleViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800"
    Name="LocalControl">

    <StatusBar Margin="{DynamicResource App.Layout.Padding.Bottom}">
        <StatusBar.Resources>
            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Reveal}">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="8,0,0,0"/>
            </Style>
        </StatusBar.Resources>

        <StatusBarItem DockPanel.Dock="Right">
            <Button
                Content="{StaticResource App.Icon.Close}"
                Command="{Binding ToggleCustomFilterCommand}"/>
        </StatusBarItem>

        <StatusBarItem DockPanel.Dock="Right" tk:Property.IsVisible="{Binding ShowClearButton, ElementName=LocalControl}">
            <Button
                Content="{StaticResource App.Icon.Filter.Off}"
                Command="{Binding ClearFilterCommand}"/>
        </StatusBarItem>

        <StatusBarItem>
            <TextBlock Text="{Binding Title, ElementName=LocalControl}"/>
        </StatusBarItem>

    </StatusBar>

</UserControl>