<Grid
    x:Class="Restless.Panama.View.SubmissionEditMessage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:tk="http://schemas.restless.toolkit.com/2021"
    xmlns:vm="clr-namespace:Restless.Panama.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:SubmissionViewModel}"
    mc:Ignorable="d"
    tk:Property.RowHeights="*,2*"
    d:DesignHeight="600" d:DesignWidth="400">

    <tk:DataGrid
        Style="{StaticResource Panama.Styles.DataGrid.Locked}"
        Margin="{DynamicResource Panama.Layout.Margin.Control.Stacked}"
        ItemsSource="{Binding Messages.ListView}"
        SelectedItem="{Binding Messages.SelectedItem}"
        tk:DataGridColumns.Columns="{Binding Messages.Columns}">

        <DataGrid.InputBindings>
            <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding Messages.OpenRowCommand}"/>
        </DataGrid.InputBindings>

        <DataGrid.ContextMenu>
            <ContextMenu ItemsSource="{Binding Messages.MenuItems}"/>
        </DataGrid.ContextMenu>

    </tk:DataGrid>

    <Grid
        Grid.Row="1"
        Style="{StaticResource Panama.Styles.Panel.DataEntry}"
        tk:Property.RowHeights="Auto,Auto,*"
        Visibility="{Binding Messages.EditVisibility}">

        <Grid.Resources>
            <Style TargetType="tk:LineItem" BasedOn="{StaticResource Panama.Styles.LineItem}">
                <Setter Property="Margin" Value="{DynamicResource Panama.Layout.Margin.Control.Stacked.Tight}"/>
            </Style>
        </Grid.Resources>

        <StackPanel>
            <TextBlock Text="Subject (Display)"/>
            <TextBox Text="{Binding Messages.SelectedMessage.Display}"/>

            <tk:LineItem Item="Subject (Orig)" Value="{Binding Messages.SelectedMessage.Subject}"/>
            <tk:LineItem Item="Date" Value="{Binding Messages.SelectedMessage.MessageDate}"/>
            <tk:LineItem Item="From" Value="{Binding Messages.SelectedMessage.SenderName}"/>
            <tk:LineItem Item="To" Value="{Binding Messages.SelectedMessage.RecipientName}"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Message"/>
        <TextBox
            Grid.Row="2"
            Style="{StaticResource Panama.Styles.TextBox.Multiline}"
            IsReadOnly="True"
            Text="{Binding Messages.MessageText, Mode=OneWay}"/>
    </Grid>
</Grid>