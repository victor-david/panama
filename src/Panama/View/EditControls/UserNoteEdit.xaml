<UserControl 
    x:Class="Restless.Panama.View.UserNoteEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="500" d:DesignWidth="350">
    <Border>
        <DockPanel>
            <Label DockPanel.Dock="Top">Title</Label>
            <TextBox DockPanel.Dock="Top" Text="{Binding SelectedRow[title]}" />
            <Label DockPanel.Dock="Top">Note</Label>
            <TextBox DockPanel.Dock="Top" Style="{StaticResource TextBoxMultiFull}" Text="{Binding SelectedRow[note]}" />
        </DockPanel>
    </Border>
</UserControl>
