<mah:MetroWindow
    x:Class="Restless.Panama.View.ToolWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tk="http://schemas.restless.toolkit.com/2021"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:ico="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:con="clr-namespace:Restless.Panama.Controls;assembly=Panama.Controls"
    xmlns:r="clr-namespace:Restless.Panama.Resources;assembly=Panama.Resources"
    xmlns:vm="clr-namespace:Restless.Panama.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:ToolWindowViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="610"
    ResizeMode="CanResizeWithGrip"
    Title="{x:Static r:Menu.Tools}"
    Style="{StaticResource App.Styles.MetroWindow}"
    Height="{Binding Config.ToolWindowHeight, Mode=TwoWay}"
    Width="{Binding Config.ToolWindowWidth, Mode=TwoWay}">

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands Margin="16,0" ShowSeparators="False">

            <mah:WindowCommands.Resources>
                <Style TargetType="ico:PackIconMaterial" BasedOn="{StaticResource App.Styles.Icon.TitleBar}"/>
            </mah:WindowCommands.Resources>

            <Button
                Command="{Binding ResetWindowCommand}"
                ToolTip="{x:Static r:ToolTip.ResetWindow}">
                <ico:PackIconMaterial Kind="Monitor"/>
            </Button>

        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <Grid tk:Property.ColumnWidths="126,*" tk:Property.RowHeights="*">
        <ListBox
            Margin="{StaticResource App.Layout.Padding.Vertical}"
            BorderThickness="0,0,1,0"
            ItemsSource="{Binding Sections}"
            DisplayMemberPath="Name"
            SelectedItem="{Binding SelectedSection}"/>

        <!-- Wait panel for any operation -->
        <tk:WaitPanel
            Margin="{StaticResource App.Layout.Padding.Bottom}"
            BorderThickness="0"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Center"
            tk:Property.IsVisible="{Binding IsOperationInProgress}"/>

        <Grid
            Grid.Column="1"
            Margin="{DynamicResource App.Layout.Padding.Full}"
            Style="{StaticResource App.Styles.Panel.DataEntry}">

            <Panel.Resources>
                <Style TargetType="con:UpdateToolControl">
                    <Setter Property="ButtonStyle">
                        <Setter.Value>
                            <Style TargetType="Button" BasedOn="{StaticResource App.Styles.Button.Dialog}">
                                <Setter Property="MinWidth" Value="0"/>
                                <Setter Property="tk:Property.IsInverseEnabled" Value="{Binding IsOperationInProgress}"/>
                            </Style>

                        </Setter.Value>
                    </Setter>
                    <Setter Property="ButtonContent" Value="{x:Static r:Text.Start}"/>
                </Style>
                <Style TargetType="ListBox" BasedOn="{StaticResource MahApps.Styles.ListBox}">
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.TextBox.Border}"/>
                </Style>
            </Panel.Resources>

            <!-- Section 1 (title metadata) -->
            <con:UpdateToolControl
                Mode="Standard"
                Header="{x:Static r:Header.TitleMetadata}"
                ButtonCommand="{Binding TitleMetaCommand}"
                ExplanationText="{x:Static r:Detail.TitleMetadata}"
                FolderText="{Binding Config.FolderTitleRoot, Mode=OneTime}"
                UpdatedText="{x:Static r:Header.Updated}"
                NotFoundText="{x:Static r:Header.NotFound}"
                StatusText="{Binding Adapter.Status[0]}"
                UpdatedItemsSource="{Binding Adapter.Updated[0]}"
                NotFoundItemsSource="{Binding Adapter.NotFound[0]}"
                tk:Property.IsLongVisible="{Binding SelectedSection.Id}"
                tk:Property.IsLongVisibleValue="1"/>

            <!-- Section 2 (submission metadata) -->
            <con:UpdateToolControl
                Mode="Standard"
                Header="{x:Static r:Header.SubmissionMetadata}"
                ButtonCommand="{Binding SubmissionMetaCommand}"
                ExplanationText="{x:Static r:Detail.SubmissionMetadata}"
                FolderText="{Binding Config.FolderSubmissionDocument, Mode=OneTime}"
                UpdatedText="{x:Static r:Header.Updated}"
                NotFoundText="{x:Static r:Header.NotFound}"
                StatusText="{Binding Adapter.Status[1]}"
                UpdatedItemsSource="{Binding Adapter.Updated[1]}"
                NotFoundItemsSource="{Binding Adapter.NotFound[1]}"
                tk:Property.IsLongVisible="{Binding SelectedSection.Id}"
                tk:Property.IsLongVisibleValue="2"/>

            <!-- Section 3 (export) -->
            <con:UpdateToolControl
                Mode="Standard"
                Header="{x:Static r:Header.Export}"
                ButtonCommand="{Binding TitleExportCommand}"
                ExplanationText="{x:Static r:Detail.Export}"
                FolderText="{Binding Config.FolderExport, Mode=OneTime}"
                UpdatedText="{x:Static r:Header.AddedUpdated}"
                NotFoundText="{x:Static r:Header.Removed}"
                StatusText="{Binding Adapter.Status[2]}"
                UpdatedItemsSource="{Binding Adapter.Updated[2]}"
                NotFoundItemsSource="{Binding Adapter.NotFound[2]}"
                tk:Property.IsLongVisible="{Binding SelectedSection.Id}"
                tk:Property.IsLongVisibleValue="3"/>

            <!-- Section 4 (title list) -->
            <con:UpdateToolControl
                Mode="Text"
                Header="{x:Static r:Header.TitleList}"
                ButtonCommand="{Binding TitleListCommand}"
                ExplanationText="{x:Static r:Detail.TitleList}"
                FolderText="{Binding TitleListFileName, Mode=OneTime}"
                OutputText="{Binding Adapter.OutputText[3]}"
                StatusText="{Binding Adapter.Status[3]}"
                tk:Property.IsLongVisible="{Binding SelectedSection.Id}"
                tk:Property.IsLongVisibleValue="4"/>

            <!-- Section 5 (messages) -->
            <con:UpdateToolControl
                Mode="Text"
                Header="{x:Static r:Header.Messages}"
                ButtonCommand="{Binding MessageSyncCommand}"
                ExplanationText="{x:Static r:Detail.Message}"
                FolderText="{Binding Config.FolderSubmissionMessage}"
                OutputText="{Binding Adapter.OutputText[4]}"
                StatusText="{Binding Adapter.Status[4]}"
                tk:Property.IsLongVisible="{Binding SelectedSection.Id}"
                tk:Property.IsLongVisibleValue="5"/>

        </Grid>
    </Grid>
</mah:MetroWindow>