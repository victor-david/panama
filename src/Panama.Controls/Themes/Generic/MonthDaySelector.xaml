<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:con="clr-namespace:Restless.Panama.Controls">

    <!-- Month Selector -->
    <Style x:Key="Control.Style.ListBoxItem.MonthSelector" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="6,4,12,4"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{Binding SelectedForegroundBrush, RelativeSource={RelativeSource AncestorType=con:MonthDaySelector}}"/>
                <Setter Property="Background" Value="{Binding SelectedBackgroundBrush, RelativeSource={RelativeSource AncestorType=con:MonthDaySelector}}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Control.Style.ListBox.MonthSelector" TargetType="ListBox">
        <Setter Property="ItemContainerStyle" Value="{StaticResource Control.Style.ListBoxItem.MonthSelector}"/>
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="0"/>
            </Style>
        </Style.Resources>
    </Style>

    <!-- Day Selector -->
    <Style x:Key="Control.Style.ListBoxItem.DaySelector" TargetType="ListBoxItem" BasedOn="{StaticResource Control.Style.ListBoxItem.MonthSelector}">
        <Setter Property="Padding" Value="2"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Width" Value="24"/>
    </Style>

    <Style x:Key="Control.Style.ListBox.DaySelector" TargetType="ListBox">
        <Setter Property="ItemContainerStyle" Value="{StaticResource Control.Style.ListBoxItem.DaySelector}"/>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border Padding="4">
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="0"/>
            </Style>
        </Style.Resources>
    </Style>

    <!-- Control template -->
    <ControlTemplate x:Key="Control.Template.MonthDaySelector" TargetType="con:MonthDaySelector">

        <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            Padding="{TemplateBinding Padding}">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <ListBox
                    Style="{TemplateBinding MonthSelectorStyle}"
                    Foreground="{TemplateBinding Foreground}"
                    BorderThickness="0,0,1,0"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    ItemsSource="{TemplateBinding Months}"
                    SelectedValue="{Binding SelectedMonth, RelativeSource={RelativeSource TemplatedParent}}"
                    SelectedValuePath="Value"/>

                <ListBox
                    Grid.Column="1"
                    Style="{TemplateBinding DaySelectorStyle}"
                    Foreground="{TemplateBinding Foreground}"
                    BorderThickness="0"
                    ItemsSource="{TemplateBinding Days}"
                    SelectedValue="{Binding SelectedDay, RelativeSource={RelativeSource TemplatedParent}}" />
            </Grid>

        </Border>

    </ControlTemplate>

    <!-- Control style -->
    <Style TargetType="con:MonthDaySelector">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="BorderBrush" Value="LightGray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="MonthSelectorStyle" Value="{StaticResource Control.Style.ListBox.MonthSelector}"/>
        <Setter Property="DaySelectorStyle" Value="{StaticResource Control.Style.ListBox.DaySelector}"/>
        <Setter Property="Template" Value="{StaticResource Control.Template.MonthDaySelector}"/>
    </Style>

</ResourceDictionary>